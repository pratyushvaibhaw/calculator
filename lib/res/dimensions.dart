import 'package:flutter/material.dart';

class AppDimension {
  static double containerradius = 12;
  static double containerheight = 300;
  static Offset offset = Offset(1, 1);
  static double blur = 2.5;
}
